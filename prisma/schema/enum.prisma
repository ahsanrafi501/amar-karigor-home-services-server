enum PaymentProvider {
    sslcommerz
}

enum UserStatus {
    ACTIVE
    INACTIVE
    SUSPENDED
}

enum Role {
    USER
    TECHNICIAN
    ADMIN
}

enum VerificationStatus {
    PENDING
    VERIFIED
    REJECTED
}

enum BookingStatus {
    PENDING
    CONFIRMED
    IN_PROGRESS
    COMPLETED
    CANCELLED
}
