enum PaymentProvider {
    sslcommerz
}

enum UserStatus {
    ACTIVE
    INACTIVE
    SUSPENDED
}

enum Role {
    USER
    TECHNICIAN
    ADMIN
}

enum VerificationStatus {
    PENDING
    VERIFIED
    REJECTED
}